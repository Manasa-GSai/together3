# ═══════════════════════════════════════════════════════════════════════════════
# DEPLOYMENT - Base Kubernetes Deployment
# App: together3 | Tenant: opseratest
# Generated by Opsera Code-to-Cloud Enterprise v1.7.3
# ═══════════════════════════════════════════════════════════════════════════════

apiVersion: apps/v1
kind: Deployment
metadata:
  name: together3
  labels:
    app: together3
    tenant: opseratest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: together3
  template:
    metadata:
      labels:
        app: together3
        tenant: opseratest
    spec:
      # RULE 81: imagePullSecrets REQUIRED for private ECR registries
      imagePullSecrets:
        - name: ecr-registry-secret
      containers:
        - name: together3
          image: together3:latest  # Replaced by Kustomize
          ports:
            - containerPort: 8080
              protocol: TCP
          # RULE 80: imagePullPolicy: Always is REQUIRED for Argo Rollouts
          imagePullPolicy: Always
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
