# ═══════════════════════════════════════════════════════════════════════════════
# ARGOCD APPLICATION - Dev Environment (DIRECT ELB MODE)
# App: together3 | Tenant: opseratest | Env: dev
# Generated by Opsera Code-to-Cloud Enterprise v1.7.3
# ═══════════════════════════════════════════════════════════════════════════════

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: together3-dev
  namespace: argocd
  labels:
    app: together3
    tenant: opseratest
    environment: dev
    lb-mode: direct-elb
spec:
  project: default

  source:
    repoURL: PLACEHOLDER_REPO_URL  # Will be replaced by bootstrap
    targetRevision: HEAD
    path: .opsera-together3/k8s/overlays/dev

  destination:
    # RULE 59: Use 'name' for spoke cluster, NOT 'server'
    name: opseratest-use1-np
    namespace: opseratest-together3-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
